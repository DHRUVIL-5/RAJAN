# RAJAN — Python dependencies
# Install: pip install -r requirements.txt

# HTTP requests (used across all agents)
requests>=2.28.0

# Rich terminal output (colors, progress bars)
rich>=13.0.0

# SQLite is stdlib — listed here for documentation only
# sqlite3  # built-in, no install needed

# Optional — install for faster scans (auto-detected at runtime)
# dnspython>=2.3.0       # Better DNS resolution
# python-whois>=0.8.0    # Richer WHOIS parsing

# All other dependencies use Python stdlib only:
# urllib.request, socket, threading, json, re, ssl,
# subprocess, sqlite3, queue, time, os, sys, datetime
